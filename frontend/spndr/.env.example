# Backend API base URL (must match backend PORT, default 5000)
VITE_API_URL=http://localhost:5000/api/v1

# Offline local-first sync engine (Phase 13). Keep unset/false until page migration (13.9) lands -
# the online-only path is still the default while this rolls out.
VITE_LOCAL_FIRST=false
